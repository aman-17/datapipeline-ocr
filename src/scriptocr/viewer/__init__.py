"""Browse every page in the store and hand-pick the SFT mixture.

`socr viewer` serves a local web app: documents grouped by source, every page
rendered on demand, and one click per page to put it in (or take it out of) the
selection folder on the Desktop. Stage 2 signals — text layer, forms, sampled
table/chart density — are exposed as filters so half a million pages can be
navigated by what they contain rather than scrolled through.
"""
